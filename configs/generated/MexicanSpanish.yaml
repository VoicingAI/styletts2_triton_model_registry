# Generated by tools/generate_configs.py from speaker_configs.json +
# speaker_languages.json. Review before use -- in particular `plbert`, which is
# inferred from the speakers' languages.
#
#   python convert.py configs/generated/MexicanSpanish.yaml --dry-run


model_id: MexicanSpanish

stores:
  prod:
    type: s3
    bucket: verizon-audio-main
    access_key_env: AWS_ACCESS_KEY
    secret_key_env: AWS_SECRET_KEY
  b2:
    type: s3
    bucket: TTS-STT-servers
    endpoint_url: https://s3.us-east-005.backblazeb2.com
    access_key_env: B2_KEY_ID
    secret_key_env: B2_APP_KEY

defaults:
  store: prod

checkpoint:
  model: prodttsmodels/mexican/model.pth
  config: prodttsmodels/mexican/config_ft.yml

plbert:
  store: b2
  path: PLBERTs/multi

speakers:
  sam:
    reference: prodttsmodels/mexican/reference.wav
    language: es

export:
  device: cuda:0
  opset: 17
  modules: all
