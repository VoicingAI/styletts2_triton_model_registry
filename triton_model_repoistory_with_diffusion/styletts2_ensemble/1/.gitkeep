An ensemble carries no weights, but Triton still requires a version
directory. Git cannot track an empty directory, so this file exists to keep
styletts2_ensemble/1/ present in a clone. Safe to ignore at runtime.
